#!/usr/bin/env -S deno run --allow-read --allow-env

/**
 * Test runner for JustTCG API helpers
 * Run with: deno run --allow-env supabase/functions/justtcg-sync/run-tests.ts
 */

import './api-helpers.test.ts';